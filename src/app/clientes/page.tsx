import { ScreenPlaceholder } from "@/components/screen-placeholder";

// /clientes — P2, lista de clientes con buscador.
export default function Page() {
  return (
    <ScreenPlaceholder
      screen="P2 — Lista de clientes"
      functions="F2 — Buscador"
      ticket="ARC-9, ARC-34 (prioridad)"
    />
  );
}
