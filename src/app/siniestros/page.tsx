import { ScreenPlaceholder } from "@/components/screen-placeholder";

// /siniestros — P5, panel de siniestros.
export default function Page() {
  return (
    <ScreenPlaceholder
      screen="P5 — Panel de siniestros"
      functions="F11 — Panel de siniestros"
      ticket="ARC-22"
    />
  );
}
