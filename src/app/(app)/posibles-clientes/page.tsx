import { ScreenPlaceholder } from "@/components/screen-placeholder";

// /posibles-clientes — P6 (Mejora 5). Filtro sobre D1 (saleStatus = "interesado").
export default function Page() {
  return (
    <ScreenPlaceholder
      screen="P6 — Posibles clientes"
      functions="F14 — Posibles clientes"
      ticket="ARC-57, ARC-58 (nav)"
    />
  );
}
